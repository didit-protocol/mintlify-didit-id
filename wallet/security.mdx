---
title: "Wallet security"
description: "The wallet's security model — holder key binding, single-use codes and nonces, transaction codes, and what the wallet never sees."
---

This page will cover the wallet's security guarantees: credentials bound to holder keys that never leave the device, single-use pre-authorized codes, access tokens, and nonces, brute-force-protected transaction codes, selective disclosure by default, and the boundary that keeps tenant API keys strictly server-side.

<Note>Content lands with the wallet release.</Note>
