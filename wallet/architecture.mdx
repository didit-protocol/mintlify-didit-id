---
title: "Wallet architecture"
description: "How the holder wallet is built — key storage, the OpenID4VCI claim pipeline, and the OpenID4VP presentation pipeline."
---

This page will describe the wallet's moving parts: per-credential holder keys and where they live, the claim pipeline (offer preview, token exchange, nonce, `openid4vci-proof+jwt`, credential storage), the presentation pipeline (DCQL matching, consent, KB-JWT signing), and how the wallet talks to the public protocol endpoints without ever seeing a tenant API key.

<Note>Content lands with the wallet release.</Note>
