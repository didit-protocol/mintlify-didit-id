---
title: "Enable or disable a trusted issuer (disabled fails the trust gate but keeps the entry)."
sidebarTitle: "Enable / disable issuer"
seo:
  title: "Enable or disable a trusted issuer (disabled fails the trust gate but keeps the entry). — Didit ID API"
  description: "PATCH /v1/trusted-issuers/{uuid} — Enable or disable a trusted issuer (disabled fails the trust gate but keeps the entry).. Didit ID verifiable-credentials API (OpenID4VCI / OpenID4VP, SD-JWT VC)."
openapi: "PATCH /v1/trusted-issuers/{uuid}"
---
